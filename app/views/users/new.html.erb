<br><h1 class="text-center text-danger">🖱Sign in🖱</h1> <br>
<%= form_for @user, url: {action: "create"} do |f| %>
<div class="container jumbotron" >
  <%= f.text_field :name, class: "form-control", placeholder: "name" %><br>
  <%= f.text_field :email, class: "form-control", placeholder: "email" %><br>
  <%= f.text_area :password, class: "form-control", placeholder: "password" %><br>
  <%= f.submit "Create User", class: "btn btn-warning" %>
  </div>
<% end %>
